name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  repo-sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Basic sanity checks
        run: |
          test -f README.md
          test -f .coderabbit.yaml
